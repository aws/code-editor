Index: code-editor-src/build/gulpfile.vscode.web.ts
===================================================================
--- code-editor-src.orig/build/gulpfile.vscode.web.ts
+++ code-editor-src/build/gulpfile.vscode.web.ts
@@ -167,6 +167,10 @@ function packageTask(sourceFolderName: s
 			gulp.src('resources/server/code-192.png', { base: 'resources/server' }),
 			gulp.src('resources/server/code-512.png', { base: 'resources/server' })
 		);
+		const indexHtml = gulp.src('resources/server/index.html', { base: 'resources/server' });
+
+		const productJsonStream = gulp.src(['product.json'], { base: '.' })
+			.pipe(jsonEditor({ commit, date: readISODate('out-build'), version }));
 
 		const all = es.merge(
 			packageJsonStream,
@@ -175,7 +179,9 @@ function packageTask(sourceFolderName: s
 			deps,
 			favicon,
 			manifest,
-			pwaicons
+			pwaicons,
+			indexHtml,
+			productJsonStream
 		);
 
 		const result = all
